user_str = input("Please enter a string: ")#collected string
length_str = len(user_str)
print(user_str[:length_str // 2].lower() + user_str[length_str // 2:].upper())#print first helf in lower
# leters and second helf in upper letters
#### if the length of str not slicing in helf
#### the lower letters small in one from the upper leters

